apiVersion: v1
kind: Service

metadata:
  name: onlineexam-service

spec:
  type: NodePort

  selector:
    app: onlineexam

  ports:
  - protocol: TCP
    port: 80
    targetPort: 4173
    nodePort: 30007